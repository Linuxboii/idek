{
  "name": "spacelink-infra",
  "private": true,
  "version": "1.0.0",
  "description": "Read-only SpaceLink Infra lead dashboard backed by the shared SLI-LG API",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "start": "npm --prefix client run preview",
    "server": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "exceljs": "^4.4.0",
    "express": "^4.19.2",
    "multer": "^2.1.1",
    "pg": "^8.11.5"
  }
}
